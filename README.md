# example-many-branches
An example repo with a lot of branches. May be useful to test your Github scraper/hooks.

You can get all of the branches locally with:

```
git fetch --all
git pull --all
```
